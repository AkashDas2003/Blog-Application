<!DOCTYPE html>
<html lang="en">

<head>
              <meta charset="UTF-8" />
              <meta name="viewport" content="width=device-width, initial-scale=1.0" />
              <title>Document</title>
</head>

<body>
              <div class="w-full p-2 h-screen text-white bg-zinc-900 text-center">
                            <div class="flex justify-end w-full">
                                          <a  class="bg-red-500 hover:bg-red-600 text-black font-bold py-2 px-4 rounded-xl mt-5 transition duration-300 ease-in-out transform hover:scale-105"
                                                        href="/logout">Logout</a>
                            </div>
                            <h5 class="mt-5 uppercase font-bold text-4xl">Edit Your Post</h5>
                            <form action="/update/<%=post._id%>"  autocomplete="off" method="post">

                                          <textarea name="content" placeholder=" Edit Your Post !!🤔🤔"
                                                        class="m-5 w-1/3 p-3 outline-none bg-transparent border-2 border-zinc-800"><%=post.content%></textarea>

                                          <input type="submit" value="Update Post"
                                          class="bg-yellow-500 hover:bg-yellow-600 text-black font-bold py-2 px-4 rounded-xl mt-5 transition duration-300 ease-in-out transform hover:scale-105" />
                            </form>
<script src="https://cdn.tailwindcss.com"></script>
</body>

</html>
